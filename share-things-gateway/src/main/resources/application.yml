############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token前缀
  token-prefix: Bearer
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  # 30天
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: false
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: random-128
  # 是否输出操作日志
  is-log: true

SMS:
  name: ali

aliyun:
  accessKeyId: LTAI5t99LQAabfpwWL5fPZ9q
  accessKeySecret: wjsdYIJEe9Dck6XqYmWv8j0ij4Tiad

# 1. 补全Spring数据源配置（必加，否则数据库连接失败）
spring:
  # 2. Spring原生Redis配置（若需保留，与Redisson配置保持一致）
  data:
    redis:
      database: 6
      host: 47.108.58.72
      port: 6379
      password: zhangzc123
      timeout: 5s
      connect-timeout: 5s
      lettuce:
        pool:
          max-active: 200
          max-wait: -1ms
          min-idle: 0
          max-idle: 10
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # 指定数据库驱动类
    # 数据库连接信息
    url: jdbc:mysql://47.108.58.72:3307/share_all?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: mikasa # 数据库用户名
    password: zhangzc123 # 数据库密码
    type: com.alibaba.druid.pool.DruidDataSource
    druid: # Druid 连接池
      initial-size: 5 # 初始化连接池大小
      min-idle: 5 # 最小连接池数量
      max-active: 20 # 最大连接池数量
      max-wait: 60000 # 连接时最大等待时间（单位：毫秒）
      test-while-idle: true
      time-between-eviction-runs-millis: 60000 # 配置多久进行一次检测，检测需要关闭的连接（单位：毫秒）
      min-evictable-idle-time-millis: 300000 # 配置一个连接在连接池中最小生存的时间（单位：毫秒）
      max-evictable-idle-time-millis: 900000 # 配置一个连接在连接池中最大生存的时间（单位：毫秒）
      validation-query: SELECT 1 FROM DUAL # 配置测试连接是否可用的查询 sql
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: false
      web-stat-filter:
        enabled: true
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/* # 配置监控后台访问路径
        login-username: admin # 配置监控后台登录的用户名、密码
        login-password: admin
      filter:
        config:
          enabled: true
        stat:
          enabled: true
          log-slow-sql: true # 开启慢 sql 记录
          slow-sql-millis: 2000 # 若执行耗时大于 2s，则视为慢 sql
          merge-sql: true
        wall: # 防火墙
          config:
            multi-statement-allow: true
# 3. Redisson配置：改为顶级节点，删除codec标签
redisson:
  singleServerConfig:
    idleConnectionTimeout: 10000  # 连接空闲超时（毫秒）
    connectTimeout: 5000  # 与Spring Redis超时对齐（5秒）
    timeout: 5000  # 命令等待超时（5秒）
    retryAttempts: 3  # 重试次数
    retryInterval: 1500  # 重试间隔（毫秒）
    password: zhangzc123  # 与Spring Redis密码一致
    subscriptionsPerConnection: 5  # 单个连接最大订阅数
    address: redis://47.108.58.72:6379  # 与Spring Redis地址一致
    subscriptionConnectionMinimumIdleSize: 1  # 订阅连接最小空闲数
    subscriptionConnectionPoolSize: 50  # 订阅连接池大小
    connectionMinimumIdleSize: 32  # 普通连接最小空闲数
    connectionPoolSize: 64  # 普通连接池大小
    database: 5  # 与Spring Redis数据库一致
    dnsMonitoringInterval: 5000  # DNS监测间隔（毫秒）
  transportMode: NIO  # 传输模式（无需修
server:
  port: 1234

zhangzc:
  leaf:
    segmentEnable: true
    snowflake-enable: true
    jdbcUrl: jdbc:mysql://47.108.58.72:3307/leaf?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    snowflake-zk-address: 119.45.4.154:2181
    jdbc-username: mikasa
    jdbc-password: zhangzc123
  listener:
    enableRedis: true